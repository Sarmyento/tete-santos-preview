{
  "name": "tete-santos-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Site Tetê Santos · Heritage Real · P4 scaffold",
  "scripts": {
    "prepare": "node scripts/setup-git-hooks.mjs",
    "dev": "astro dev",
    "optimize:static": "node scripts/optimize-static-images.mjs",
    "prebuild": "node scripts/optimize-static-images.mjs && node scripts/fetch-listings.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "apply-brand": "node scripts/apply-brand.mjs",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@fontsource/fraunces": "^5.2.9",
    "@fontsource/spectral": "^5.2.8",
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "sharp": "^0.34.2"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
