---
import BaseLayout from '../layouts/BaseLayout.astro';
import ServicesGrid from '../components/sections/ServicesGrid.astro';
import CtaBand from '../components/sections/CtaBand.astro';
import RevealInit from '../components/sections/RevealInit.astro';
import { site } from '../config/site';
---

<BaseLayout
  title={`Serviços | ${site.name}`}
  description={`Compra, venda, avaliação e captação de imóveis de alto luxo com ${site.name}.`}
>
  <section class="page-hero section reveal">
    <div class="container page-hero__inner">
      <p class="eyebrow">Serviços</p>
      <h1>Consultoria completa para patrimônios de alto padrão</h1>
      <p class="page-hero__lead">
        Cada serviço é conduzido com método, sensibilidade estética e foco em resultado.
      </p>
    </div>
  </section>

  <ServicesGrid compact={true} />

  <section class="section reveal">
    <div class="container container--narrow process">
      <p class="eyebrow">Como funciona</p>
      <h2>Um processo claro, do primeiro contato ao fechamento</h2>
      <ol>
        <li>Diagnóstico consultivo do objetivo e do perfil do imóvel ou da busca.</li>
        <li>Estratégia de posicionamento, precificação e narrativa premium.</li>
        <li>Curadoria de visitas, materiais e condução das negociações.</li>
        <li>Acompanhamento próximo até a assinatura com segurança jurídica.</li>
      </ol>
    </div>
  </section>

  <CtaBand />
  <RevealInit />
</BaseLayout>

<style>
  .page-hero {
    padding-top: calc(var(--header-height) + var(--space-6));
  }

  .page-hero__inner {
    max-width: 760px;
  }

  .page-hero__lead {
    color: var(--color-muted);
    max-width: 52ch;
  }

  .process ol {
    margin: var(--space-4) 0 0;
    padding-left: 1.2rem;
    color: var(--color-muted);
    display: grid;
    gap: var(--space-2);
  }
</style>
