name: Deploy preview to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      PUBLIC_DEPLOY_TARGET: github-pages
      PUBLIC_SITE_URL: https://sarmyento.github.io/tete-santos-preview
      PUBLIC_BASE_PATH: /tete-santos-preview/
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # LISTINGS_FEED_URL no Build: o prebuild (fetch-listings.mjs) sobrescreve
      # listings.xml — sem o secret aqui o mock vencia o curl do passo antigo.
      - name: Build site
        env:
          LISTINGS_FEED_URL: ${{ secrets.LISTINGS_FEED_URL }}
        run: npm run build

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
